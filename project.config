[project]
	description = Check active calls and terminate them if calls are 'lost' due to server crash.
[submit]
	action = fast forward only
	mergeContent = false
[access]
	inheritFrom = All+Sipwise+Developers+NGCP-Projects
